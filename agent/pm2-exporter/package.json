{
  "name": "opspika-process-exporter",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only Prometheus exporter for a PM2 daemon.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check metrics.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
